name: T# CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make install
      run: sudo make install
    - name: run
      run: t# helloworld.t#
    - name: clean
      run: sudo make clean;
